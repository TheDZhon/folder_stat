#include "folder_stat_window.h"

FolderStatWindow::FolderStatWindow(QWidget *parent, Qt::WFlags flags)
	: QMainWindow(parent, flags)
{
	ui.setupUi(this);
}

FolderStatWindow::~FolderStatWindow()
{

}
